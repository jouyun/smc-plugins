# Name: Process Pixels
# Author: The Fiji Team
# Version: 1.0.0

# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

Plugins>ABC, "My tabletop", splugins.My_Plugins
Plugins>SMC, "add channel", splugins.add_channel
Plugins>SMC, "Add Stripey Tiles", splugins.Add_Stripey_Tiles
Plugins>SMC, "Align In Z", splugins.Align_In_Z
Plugins>SMC, "Bin Image", splugins.Bin_Image
Plugins>SMC, "Calculate Masked Pearsons", splugins.Calculate_Masked_Pearsons
Plugins>SMC, "Calculate Spatial Correlation", splugins.Calculate_Spatial_Correlation
Plugins>SMC, "Correct Flatness", splugins.Correct_Flatness
Plugins>SMC, "Dot Source Target Masks", splugins.Dot_Source_Target_Masks
Plugins>SMC, "FileStackReg", splugins.FileStackReg_
Plugins>SMC, "FileStackReg2", splugins.FileStackReg2_
Plugins>SMC, "FileStackReg3", splugins.FileStackReg3_

